version: 2

models:
  - name: pkg_sts_compare
    columns:
      - name: package_name
        tests:
          - relationship:
              to: ref('pkg_sts_projection_tracking')
              field: package_name